const fonts = {
    sans: 'system-ui, sans-serif',
    mono: '"JetBrains Mono", Menlo, monospace',
}

const fontSizes = {
    xxs: 8,
    xs: 12,
    s: 14,
    m: 16,
    l: 20,
    xl: 24,
    xxl: 32,
}

export default {
    fonts,
    sizes: fontSizes,
}